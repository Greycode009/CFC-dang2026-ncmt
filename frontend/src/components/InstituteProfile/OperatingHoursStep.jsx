import React from "react";
import ContactDetails from "./ContactDetails";

const OperatingHoursStep = ({ formData, handleChange }) => {
  return <ContactDetails formData={formData} handleChange={handleChange} />;
};

export default OperatingHoursStep;
