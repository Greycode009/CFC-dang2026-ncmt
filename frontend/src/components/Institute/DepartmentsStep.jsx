import React from "react";
import Services from "./Services";

const DepartmentsStep = ({ formData, handleChange }) => {
  return <Services formData={formData} handleChange={handleChange} />;
};

export default DepartmentsStep;
