import React from "react";
import InstituteBasics from "./InstituteBasics";

const FacilityBasicsStep = ({ formData, handleChange }) => {
  return <InstituteBasics formData={formData} handleChange={handleChange} />;
};

export default FacilityBasicsStep;
